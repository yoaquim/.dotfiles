# Test Plan: {{FEATURE_NAME}}

**Feature**: {{FEATURE_NUMBER}}-{{FEATURE_NAME}}
**Created**: {{INIT_DATE}}

---

## Scope

**In Scope**:
- [Core functionality from feature requirements]
- [User flows]
- [Error handling]

**Out of Scope**:
- [What's not being tested]

---

## Test Scenarios

### Happy Path: [Primary Flow]

**Steps**:
1. [Action]
2. [Action]
3. [Verify result]

**Playwright**:
```typescript
test('primary flow', async ({ page }) => {
  await page.goto('/path');
  await page.click('[data-testid="button"]');
  await expect(page.locator('[data-testid="result"]')).toBeVisible();
});
```

---

### Error Handling: [Error Condition]

**Steps**:
1. [Trigger error]
2. [Verify error message]

**Playwright**:
```typescript
test('handles error', async ({ page }) => {
  // Trigger error condition
  await expect(page.locator('[data-testid="error"]')).toHaveText('Expected error');
});
```

---

### Edge Case: [Edge Condition]

**Steps**:
1. [Setup edge condition]
2. [Verify behavior]

**Playwright**:
```typescript
test('edge case', async ({ page }) => {
  // Edge case test
});
```

---

## Acceptance Criteria Mapping

| Criteria | Test Scenario |
|----------|---------------|
| [AC from feature] | Happy Path |
| [AC from feature] | Error Handling |

---

## Test Data

| Data | Purpose |
|------|---------|
| [Test data] | [Which scenario uses it] |

---

## Generated Spec File

**File**: `tests/e2e/{{FEATURE_SLUG}}.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test.describe('{{FEATURE_NAME}}', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('{{BASE_URL}}');
  });

  // Add test scenarios here
});
```

---

## Results

| Scenario | Status |
|----------|--------|
| Happy Path | Pending |
| Error Handling | Pending |
| Edge Case | Pending |

---

## Notes

[Observations, issues found, recommendations]
