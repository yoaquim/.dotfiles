#---------------------
# SET TAB AS PREFIX
#---------------------
set -g prefix `                   # use ` as prefix
bind `     send-key `             # insert ` by pressing ` twice

#---------------------
# KEYBINDS
#---------------------
bind ,     previous-window
bind .     next-window
bind m     command-prompt 'move-window -t %%'
bind h     select-pane -L
bind h     select-pane -L
bind l     select-pane -R
bind k     select-pane -U
bind j     select-pane -D
bind \\    split-window -h
bind -     split-window -v        # split into top and bottom panes
bind o     rotate-window          # rotate panes around in current window
bind Left  resize-pane -L 5
bind Right resize-pane -R 5
bind Up    resize-pane -U 5
bind Down  resize-pane -D 5
bind V     select-layout even-horizontal
bind B     select-layout even-vertical
bind t     select-layout tiled
bind n     command-prompt 'rename-window %%'
bind N     command-prompt 'rename-session %%'
bind x     kill-pane
bind X     kill-window
bind Space command-prompt
bind r     source-file ~/.tmux.conf \; display-message "*** Reloaded TMUX Config ***"

#---------------------
# BEHAVIOR
#---------------------
set  -g mouse on
set  -g base-index 1          # start window numbering from 1 instead of 0
set  -g set-titles on         # auto set the terminal's window title
setw -g mode-keys vi
setw -g automatic-rename on
setw -g xterm-keys on

# appearance
set  -g default-terminal "screen-256color"

#---------------------
# COPY-PASTE
#---------------------
# Copy with Mouse
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

# TODO: FIX, NOT WORKING
# bind -T copy-mode-vi v send -X begin-selection
# bind -T copy-mode-vi b send-keys -X copy-pipe-and-cancel "pbcopy"
# bind p paste-buffer

#---------------------
# STYLING 
#---------------------
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

#  modes
setw -g clock-mode-colour colour5
setw -g mode-style 'fg=colour1 bg=colour18 bold'

# panes
set -g pane-border-style 'fg=colour19 bg=colour0'
set -g pane-active-border-style 'bg=colour0 fg=colour9'

# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-style 'bg=colour18 fg=colour137'
set -g status-left ' '
set -g status-right '#[fg=colour233,bg=colour8] %a,%b/%d/%Y #[bold]%I:%M%p #[fg=colour233,bg=colour19 bold] UTC #(TZ="UTC" date +%%H:%%M) '

setw -g window-status-current-style 'fg=colour1 bg=colour19 bold'
setw -g window-status-current-format ' #I#[fg=colour249]:#[fg=colour255]#W#[fg=colour249]#F '

setw -g window-status-style 'fg=colour9 bg=colour18'
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

setw -g window-status-bell-style 'fg=colour255 bg=colour1 bold'

# messages
set -g message-style 'fg=colour232 bg=colour16 bold'

#-----------------------------------------
# PLUGINS
#-----------------------------------------

# Plugin manager: https://github.com/tmux-plugins/tpm
set -g @plugin 'tmux-plugins/tpm'

 # List of plugins
set -g @plugin 'tmux-plugins/tmux-continuum'

# Plugin Settings
set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
